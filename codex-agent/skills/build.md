name: build
description: Implement one planned increment with minimal diff.
steps:
  - Implement ONLY the next planned increment.
  - Add/adjust tests for any new behavior.
  - Update docs/changelog briefly if needed.
