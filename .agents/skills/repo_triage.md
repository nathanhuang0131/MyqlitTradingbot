name: repo_triage
description: Understand the codebase quickly and identify blockers.
steps:
  - Identify entrypoints (CLI, main modules, services).
  - List current test command(s) and how to run.
  - Run a quick sanity command (e.g., unit tests or minimal import).
  - Summarize top 3 risks and quick wins.